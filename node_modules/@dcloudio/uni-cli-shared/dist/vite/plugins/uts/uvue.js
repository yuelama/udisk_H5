"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.uniUVueTypeScriptPlugin = exports.uniUTSUVueJavaScriptPlugin = void 0;
const magic_string_1 = __importDefault(require("magic-string"));
const compiler_core_1 = require("@vue/compiler-core");
const compiler_dom_1 = require("@vue/compiler-dom");
const vue_1 = require("../../../vue");
const SCRIPT_OPEN_TAG_RE = /<script([^>]*)>/gi;
const SCRIPT_LANG_RE = /(^|\s)lang\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s"'=<>`]+))/i;
function findScriptTag(code, node) {
    const start = node.loc.start.offset;
    let quote = '';
    let end = -1;
    for (let index = start + '<script'.length; index < code.length; index++) {
        const char = code[index];
        if (quote) {
            if (char === quote) {
                quote = '';
            }
        }
        else if (char === '"' || char === "'") {
            quote = char;
        }
        else if (char === '>') {
            end = index + 1;
            break;
        }
    }
    if (end < 0) {
        return;
    }
    const attrs = node.props.filter((prop) => prop.type === compiler_core_1.NodeTypes.ATTRIBUTE);
    const getAttr = (name) => attrs.find((attr) => attr.name.toLowerCase() === name);
    const langAttr = getAttr('lang');
    return {
        end,
        setup: !!getAttr('setup'),
        vapor: !!getAttr('vapor'),
        src: !!getAttr('src'),
        lang: langAttr?.value?.content,
        langAttr,
    };
}
function uniUTSUVueJavaScriptPlugin(options = {}) {
    process.env.UNI_UTS_USING_ROLLUP = 'true';
    const isDom2 = process.env.UNI_APP_X_DOM2 === 'true';
    const enableVaporScriptLang = isDom2 && process.env.UNI_APP_X_VAPOR_SCRIPT_LANG === 'true';
    return {
        name: 'uni:uts-uvue',
        enforce: 'pre',
        configResolved(config) {
            if (enableVaporScriptLang) {
                return;
            }
            // 移除自带的 esbuild 处理 ts 文件
            const index = config.plugins.findIndex((p) => p.name === 'vite:esbuild');
            if (index > -1) {
                // @ts-expect-error
                config.plugins.splice(index, 1);
            }
        },
        transform(code, id) {
            if (!(0, vue_1.isVueSfcFile)(id)) {
                return;
            }
            const platform = process.env.UNI_PLATFORM;
            const isApp = platform === 'app' ||
                platform === 'app-plus' ||
                platform === 'app-harmony';
            const scriptTags = enableVaporScriptLang
                ? (0, compiler_dom_1.parse)(code, {
                    parseMode: 'sfc',
                    // 此阶段只识别真实脚本块，语法错误仍由后续正式 SFC 编译统一报告。
                    onError: () => { },
                })
                    .children.filter((node) => node.type === compiler_core_1.NodeTypes.ELEMENT &&
                    node.tag.toLowerCase() === 'script')
                    .map((node) => findScriptTag(code, node))
                    .filter((script) => !!script)
                : [];
            const setupScript = scriptTags.find((script) => script.setup);
            // 同一 SFC 的普通 script 与 script setup 必须使用相同语言，因此需要整组归一为 TypeScript。
            const normalizeJavaScript = enableVaporScriptLang &&
                !!setupScript &&
                !scriptTags.some((script) => script.src) &&
                (setupScript.lang === 'js' || setupScript.lang === 'ts') &&
                scriptTags.every((script) => script.lang === 'js' || script.lang === 'ts');
            const transformScriptTag = (match, attributes) => {
                let result = '';
                const langMatch = attributes.match(SCRIPT_LANG_RE);
                const lang = langMatch
                    ? langMatch[2] || langMatch[3] || langMatch[4]
                    : undefined;
                // 未声明 lang 时保持现有 UTS 默认行为。
                if (!langMatch) {
                    result = `<script${attributes} lang="uts">`;
                }
                else if (lang === 'ts') {
                    // 未启用 Vapor JS/TS 脚本时，TypeScript 继续由 uts2js 处理。
                    result = match.replace(langMatch[0], `${langMatch[1]}lang="uts"`);
                }
                else {
                    result = match;
                }
                if (isDom2 &&
                    attributes.includes('setup') &&
                    !attributes.includes('vapor')) {
                    result = result.replace(/(\s)lang(?=\s*=)/i, '$1vapor lang');
                }
                return result;
            };
            if (enableVaporScriptLang) {
                const source = id.split('?')[0];
                const transformed = new magic_string_1.default(code);
                let changed = false;
                for (const script of scriptTags) {
                    const addVapor = script.setup && !script.vapor;
                    if (script.langAttr) {
                        const normalizeLang = normalizeJavaScript && script.lang === 'js';
                        if (addVapor || normalizeLang) {
                            const langText = normalizeLang
                                ? 'lang="ts"'
                                : code.slice(script.langAttr.loc.start.offset, script.langAttr.loc.end.offset);
                            transformed.overwrite(script.langAttr.loc.start.offset, script.langAttr.loc.end.offset, `${addVapor ? 'vapor ' : ''}${langText}`);
                            changed = true;
                        }
                    }
                    else {
                        // 未声明 lang 时保持现有 UTS 默认行为，并将 vapor 放在 lang 前。
                        transformed.appendLeft(script.end - 1, `${addVapor ? ' vapor' : ''} lang="uts"`);
                        changed = true;
                    }
                }
                if (!changed) {
                    return;
                }
                return {
                    code: transformed.toString(),
                    // 阶段 map 用于后续编译诊断还原，与发行产物是否输出 sourcemap 无关。
                    map: transformed.generateMap({
                        source,
                        includeContent: true,
                        hires: 'boundary',
                    }),
                };
            }
            return {
                code: code.replace(SCRIPT_OPEN_TAG_RE, transformScriptTag),
                // app平台不可返回null，否则会报错“Multiple conflicting contents for sourcemap source”
                map: isApp ? { mappings: '' } : null,
            };
        },
    };
}
exports.uniUTSUVueJavaScriptPlugin = uniUTSUVueJavaScriptPlugin;
/**
 * 将 <script> 标签中的 lang="uts" 替换为 lang="ts"
 * 主要是当前功能内部使用 x.vite.config.ts 配置
 * @param options
 * @returns
 */
function uniUVueTypeScriptPlugin(options = {}) {
    return {
        name: 'uni:uvue-ts',
        enforce: 'pre',
        transform(code, id) {
            if (!(0, vue_1.isVueSfcFile)(id)) {
                return;
            }
            return {
                code: code.replace(/<script([^>]*)>/gi, (match, attributes) => {
                    // 如果 <script> 标签中没有 lang 属性，添加 lang="uts"
                    if (!/lang=["']?[^"']*["']?/.test(attributes)) {
                        return `<script${attributes} lang="ts">`;
                    }
                    // 否则，将现有的 lang 属性替换为 lang="uts"
                    return match.replace(/lang=["']?uts["']?/, 'lang="ts"');
                }),
                map: { mappings: '' },
            };
        },
    };
}
exports.uniUVueTypeScriptPlugin = uniUVueTypeScriptPlugin;
